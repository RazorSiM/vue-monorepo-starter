{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "prettier:check": {
      "dependsOn": ["prettier:write"]
    },
    "prettier:write": {},
    "lint:check": {
      "dependsOn": ["^build", "prettier:write", "lint:fix"],
      "inputs": ["src/**/*.{ts,js,vue}", "tests/**/*.ts", "tests/**/*.js"]
    },
    "lint:fix": {},
    "build-only": {
      "dependsOn": ["^build-only"],
      "outputs": ["dist/**"]
    },
    "start-and-generate": {},
    "build": {
      "dependsOn": ["start-and-generate", "^build"],
      "outputs": ["dist/**"]
    },
    "//#docs:build": {},
    "storybook:build": {
      "dependsOn": ["build"],
      "inputs": [
        "src/**/*.stories.ts",
        "src/**/*.mdx",
        "stories/**/*.ts",
        "stories/**/*.mdx"
      ]
    },
    "test:unit": {
      "dependsOn": ["^build"],
      "inputs": ["src/**/*/.test.ts", "tests/**/*.ts", "tests/**/*.js"]
    },
    "test:component": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**/*.ts",
        "src/**/*.vue",
        "tests/**/*.ts",
        "tests/**/*.js"
      ]
    },
    "test:e2e": {
      "dependsOn": ["^build", "build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
